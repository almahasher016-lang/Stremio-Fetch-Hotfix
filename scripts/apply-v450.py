from pathlib import Path
import json

ROOT=Path('.')
def r(p): return (ROOT/p).read_text(encoding='utf-8')
def w(p,s): (ROOT/p).write_text(s,encoding='utf-8')
def rep(s,a,b,label):
    if a not in s: raise SystemExit(f'anchor not found: {label}')
    return s.replace(a,b,1)

# Safe project-only version bump.
p='package.json'; data=json.loads(r(p)); data['version']='4.5.0'; w(p,json.dumps(data,ensure_ascii=False,indent=2)+'\n')
p='package-lock.json'; data=json.loads(r(p)); data['version']='4.5.0'; data['packages']['']['version']='4.5.0'; w(p,json.dumps(data,ensure_ascii=False,indent=2)+'\n')
p='src/release.js'; s=r(p); s=rep(s,"RELEASE_VERSION = '4.4.2'","RELEASE_VERSION = '4.5.0'",'release version'); w(p,s)

# Timing-evidence configuration is independent of the legacy experimental auto-sync gate.
p='src/configCore.js'; s=r(p)
anchor='''    accuracyPreflight: {\n      enabled: toBool(get('ACCURACY_PREFLIGHT_ENABLED'), true),\n      topN: toInt(get('ACCURACY_PREFLIGHT_TOP_N'), 5, 0, 5),\n      timeoutMs: toInt(get('ACCURACY_PREFLIGHT_TIMEOUT_MS'), 1800, 300, 8000),\n      cacheTtlSeconds: toInt(get('ACCURACY_PREFLIGHT_CACHE_TTL'), 21600, 300, 604800),\n      rejectCacheTtlSeconds: toInt(get('ACCURACY_PREFLIGHT_REJECT_CACHE_TTL'), 180, 30, 3600),\n    },\n'''
insert=anchor+'''    timingEvidence: {\n      enabled: toBool(get('TIMING_EVIDENCE_ENABLED'), true),\n      maxCues: toInt(get('TIMING_EVIDENCE_MAX_CUES'), 192, 32, 400),\n      cacheTtlSeconds: toInt(get('TIMING_EVIDENCE_CACHE_TTL'), 21600, 300, 604800),\n      minCues: toInt(get('TIMING_EVIDENCE_MIN_CUES'), 24, 4, 200),\n      minCueRatio: toNumber(get('TIMING_EVIDENCE_MIN_CUE_RATIO'), 0.62, 0, 1),\n      minTemporalAgreement: toNumber(get('TIMING_EVIDENCE_MIN_AGREEMENT'), 0.68, 0, 1),\n      minAnchorCoverage: toNumber(get('TIMING_EVIDENCE_MIN_COVERAGE'), 0.58, 0, 1),\n      maxResidualMedianMs: toInt(get('TIMING_EVIDENCE_MAX_MEDIAN_RESIDUAL_MS'), 2500, 100, 20000),\n      maxResidualP90Ms: toInt(get('TIMING_EVIDENCE_MAX_P90_RESIDUAL_MS'), 7000, 500, 60000),\n      alignedMaxOffsetMs: toInt(get('TIMING_EVIDENCE_ALIGNED_MAX_OFFSET_MS'), 1200, 0, 10000),\n      alignedMaxRatioDelta: toNumber(get('TIMING_EVIDENCE_ALIGNED_MAX_RATIO_DELTA'), 0.003, 0, 0.05),\n      safeAutoAlign: toBool(get('TIMING_EVIDENCE_SAFE_AUTO_ALIGN'), true),\n      safeMinConfidence: toInt(get('TIMING_EVIDENCE_SAFE_MIN_CONFIDENCE'), 94, 0, 100),\n      safeMinAgreement: toNumber(get('TIMING_EVIDENCE_SAFE_MIN_AGREEMENT'), 0.82, 0, 1),\n      safeMinCoverage: toNumber(get('TIMING_EVIDENCE_SAFE_MIN_COVERAGE'), 0.72, 0, 1),\n      safeMinCueRatio: toNumber(get('TIMING_EVIDENCE_SAFE_MIN_CUE_RATIO'), 0.68, 0, 1),\n      safeMaxResidualMedianMs: toInt(get('TIMING_EVIDENCE_SAFE_MAX_MEDIAN_RESIDUAL_MS'), 1500, 100, 10000),\n      safeMaxResidualP90Ms: toInt(get('TIMING_EVIDENCE_SAFE_MAX_P90_RESIDUAL_MS'), 4000, 500, 30000),\n      safeMaxOffsetMs: toInt(get('TIMING_EVIDENCE_SAFE_MAX_OFFSET_MS'), 60000, 500, 300000),\n      safeMinRatio: toNumber(get('TIMING_EVIDENCE_SAFE_MIN_RATIO'), 0.97, 0.90, 1.10),\n      safeMaxRatio: toNumber(get('TIMING_EVIDENCE_SAFE_MAX_RATIO'), 1.03, 0.90, 1.10),\n    },\n'''
s=rep(s,anchor,insert,'timing evidence config'); w(p,s)

# Reference-sync engine: add bounded timing profiles and profile-to-profile plan derivation.
p='src/utils/referenceSync.js'; s=r(p)
start=s.index('export function deriveReferenceSyncPlan(sourceText')
end=s.index('export function explainReferencePlan', start)
replacement='''function timingProfileCues(profile = {}) {\n  return Array.isArray(profile.cues)\n    ? profile.cues\n      .map(cue => ({ start: Number(cue.start), end: Number(cue.end), mid: Number(cue.mid ?? (Number(cue.start) + Number(cue.end)) / 2) }))\n      .filter(cue => Number.isFinite(cue.start) && Number.isFinite(cue.end) && cue.end > cue.start)\n    : [];\n}\n\nexport function buildTimingProfile(text = '', maxCues = 192) {\n  const cues = parseCueTimes(text);\n  const sampled = sampleCueSequence(cues, Math.max(32, Number(maxCues) || 192)).map(item => item.cue);\n  return {\n    cueCount: cues.length,\n    startMs: cues[0]?.start || 0,\n    endMs: cues.at(-1)?.end || 0,\n    durationMs: cues.length ? cues.at(-1).end - cues[0].start : 0,\n    cues: sampled.map(cue => ({ start: cue.start, end: cue.end, mid: cue.mid })),\n  };\n}\n\nexport function deriveReferenceSyncPlanFromProfiles(sourceProfile = {}, referenceProfile = {}, options = {}) {\n  const sourceCues = timingProfileCues(sourceProfile);\n  const referenceCues = timingProfileCues(referenceProfile);\n  const sourceCueCount = Number(sourceProfile.cueCount || sourceCues.length);\n  const referenceCueCount = Number(referenceProfile.cueCount || referenceCues.length);\n  const minCues = Number(options.minCues ?? 8);\n  if (sourceCueCount < minCues || referenceCueCount < minCues || sourceCues.length < 4 || referenceCues.length < 4) {\n    return { enabled: false, type: 'reference-piecewise', ratio: 1, offsetMs: 0, confidence: 0, hints: [`reference:not-enough-cues:${sourceCueCount}/${referenceCueCount}`], sourceCueCount, referenceCueCount };\n  }\n  const cueRatio = Math.min(sourceCueCount, referenceCueCount) / Math.max(sourceCueCount, referenceCueCount);\n  if (cueRatio < Number(options.minCueRatio ?? 0.55)) {\n    return { enabled: false, type: 'reference-piecewise', ratio: 1, offsetMs: 0, confidence: 0, hints: [`reference:cue-ratio-low:${cueRatio.toFixed(2)}`], sourceCueCount, referenceCueCount, cueRatio: Number(cueRatio.toFixed(3)) };\n  }\n  const temporalAnchors = buildTemporalAnchors(sourceCues, referenceCues, options.maxAnchors ?? 48);\n  const candidates = [evaluateAnchorPlan(temporalAnchors, sourceCues, referenceCues, options, 'temporal')];\n  if (options.dtwEnabled !== false) {\n    const dtwAnchors = buildDtwAnchors(sourceCues, referenceCues, {\n      maxCues: options.dtwMaxCues ?? 192,\n      maxAnchors: options.maxAnchors ?? 48,\n      bandRatio: options.dtwBandRatio ?? 0.18,\n      gapPenalty: options.dtwGapPenalty ?? 0.42,\n      maxMatchCost: options.dtwMaxMatchCost ?? 0.52,\n    });\n    candidates.push(evaluateAnchorPlan(dtwAnchors, sourceCues, referenceCues, options, 'dtw'));\n  }\n  const selected = candidates.reduce((best, candidate) => (candidateScore(candidate) > candidateScore(best) ? candidate : best));\n  return {\n    ...selected,\n    sourceCueCount,\n    referenceCueCount,\n    cueRatio: Number(cueRatio.toFixed(3)),\n  };\n}\n\nexport function deriveReferenceSyncPlan(sourceText, referenceText, options = {}) {\n  const maxCues = Number(options.profileMaxCues ?? options.dtwMaxCues ?? 192);\n  return deriveReferenceSyncPlanFromProfiles(\n    buildTimingProfile(sourceText, maxCues),\n    buildTimingProfile(referenceText, maxCues),\n    options,\n  );\n}\n\n'''
s=s[:start]+replacement+s[end:]; w(p,s)

# Encoding preflight now emits compact timing profiles; reference preflight does not apply Arabic gate.
p='src/utils/encodingProxy.js'; s=r(p)
s=rep(s,"import { deriveReferenceSyncPlan } from './referenceSync.js';","import { buildTimingProfile, deriveReferenceSyncPlan } from './referenceSync.js';",'referenceSync import')
old='''  return {\n    quality,\n    encoding: processed.encoding,\n    format: processed.format,\n    archive: extracted.archive || null,\n    archiveEntry: extracted.entryName || null,\n  };\n}\n\nasync function loadProcessedSource'''
new='''  return {\n    quality,\n    timingProfile: buildTimingProfile(processed.text, config.timingEvidence.maxCues),\n    encoding: processed.encoding,\n    format: processed.format,\n    archive: extracted.archive || null,\n    archiveEntry: extracted.entryName || null,\n  };\n}\n\nexport async function preflightTimingReferenceCandidate(item, {\n  fetcher = fetchRemoteSubtitleBuffer,\n  providerLinkResolver = defaultProviderLinkResolver,\n  signal,\n} = {}) {\n  const source = preflightSourceForItem(item);\n  const boundFetcher = (url, options = {}) => fetcher(url, { ...options, signal });\n  const buffer = await fetchSourceBuffer(source, boundFetcher, providerLinkResolver);\n  const extracted = await extractSubtitlePayload(buffer, {\n    maxDecompressedBytes: config.encodingProxy.maxDecompressedBytes,\n    maxArchiveEntries: config.encodingProxy.maxArchiveEntries,\n    sourceName: source.name,\n  });\n  const processed = processSubtitleBuffer(extracted.buffer, {\n    stripSdh: true,\n    stripMusicNotes: config.encodingProxy.stripMusicNotes,\n    sourceName: extracted.entryName || source.name,\n  });\n  assertValidProcessedSubtitle(processed.text);\n  return {\n    timingProfile: buildTimingProfile(processed.text, config.timingEvidence.maxCues),\n    encoding: processed.encoding,\n    format: processed.format,\n    archive: extracted.archive || null,\n    archiveEntry: extracted.entryName || null,\n  };\n}\n\nasync function loadProcessedSource'''
s=rep(s,old,new,'preflight timing profile'); w(p,s)

# Attach one exact-hash reference candidate derived from the actual video to every ordinary Arabic candidate.
p='src/services/subtitleServiceCore.js'; s=r(p)
old='''  const timingReferences = exactReferences.length ? exactReferences : (autoSyncEnabled ? references : []);\n  if (!timingReferences.length) return arabicResults;\n\n  return arabicResults.map(item => {\n    if (item.provider === 'registry' || item.provider === 'vault') return item;\n    const candidates = timingReferences'''
new='''  const timingReferences = exactReferences.length ? exactReferences : (autoSyncEnabled ? references : []);\n  if (!timingReferences.length) return arabicResults;\n  const primaryExactReference = exactReferences[0] || null;\n  const exactTimingReference = primaryExactReference ? {\n    provider: primaryExactReference.provider,\n    originalProvider: primaryExactReference.originalProvider,\n    providerId: primaryExactReference.fileId || primaryExactReference.providerId || primaryExactReference.id,\n    fileId: primaryExactReference.fileId || null,\n    id: primaryExactReference.id || null,\n    download: primaryExactReference.download || primaryExactReference.url || null,\n    releaseName: primaryExactReference.releaseName || primaryExactReference.fileName || primaryExactReference.name || '',\n    fileName: primaryExactReference.fileName || '',\n    lang: 'eng',\n    exactVideoHash: true,\n  } : null;\n\n  return arabicResults.map(item => {\n    if (item.provider === 'registry' || item.provider === 'vault') return item;\n    const candidates = timingReferences'''
s=rep(s,old,new,'exact timing reference attach')
old='''    let output = item;\n    if (exactHashMatch(best.reference, referenceSearch)) {'''
new='''    let output = exactTimingReference ? { ...item, exactTimingReference } : item;\n    if (exactHashMatch(best.reference, referenceSearch)) {'''
s=rep(s,old,new,'reference output'); w(p,s)

# Accuracy preflight: measure actual Arabic cue timelines against exact-hash reference.
p='src/services/accuracyPreflight.js'; s=r(p)
s=rep(s,"import { preflightSubtitleCandidate } from '../utils/encodingProxy.js';","import { preflightSubtitleCandidate, preflightTimingReferenceCandidate } from '../utils/encodingProxy.js';\nimport { deriveReferenceSyncPlanFromProfiles } from '../utils/referenceSync.js';",'preflight imports')
s=rep(s,"    archiveEntry: raw.archiveEntry || null,\n    elapsedMs:","    archiveEntry: raw.archiveEntry || null,\n    timingProfile: raw.timingProfile || null,\n    elapsedMs:",'normalize timing profile')
s=rep(s,"  if (item?.quality?.valid === true) {","  if (item?.quality?.valid === true && !(config.timingEvidence.enabled && item?.exactTimingReference)) {",'force profile inspection')
# Insert helpers before applyAccuracyPreflight.
marker='''export async function applyAccuracyPreflight(results = [], search = {}, {\n'''
helpers='''function timingReferenceKey(reference = {}) {\n  const payload = JSON.stringify({\n    provider: reference.provider || '',\n    providerId: reference.providerId || reference.fileId || reference.id || '',\n    release: reference.releaseName || reference.fileName || '',\n    policyVersion: config.app.version,\n  });\n  return `timing-reference:${createHash('sha256').update(payload).digest('hex')}`;\n}\n\nasync function inspectTimingReference(reference, { referencePreflightImpl, cacheGetImpl, cacheSetImpl } = {}) {\n  if (!reference?.exactVideoHash) return null;\n  const key = timingReferenceKey(reference);\n  const cached = await cacheGetImpl(key);\n  if (cached?.timingProfile) return { ...cached, source: 'shared-cache' };\n  const started = Date.now();\n  try {\n    const signal = AbortSignal.timeout(Math.max(config.accuracyPreflight.timeoutMs, 2200));\n    const result = await referencePreflightImpl(reference, { signal });\n    if (!result?.timingProfile) return null;\n    const outcome = { timingProfile: result.timingProfile, elapsedMs: Date.now() - started };\n    await cacheSetImpl(key, outcome, config.timingEvidence.cacheTtlSeconds, config.cache.staleSeconds);\n    return { ...outcome, source: 'live-reference' };\n  } catch {\n    return null;\n  }\n}\n\nfunction measuredTimingEvidence(plan = {}) {\n  const agreement = Number(plan.temporalAgreement || 0);\n  const coverage = Number(plan.anchorCoverage || 0);\n  const cueRatio = Number(plan.cueRatio || 0);\n  const confidence = Number(plan.confidence || 0);\n  const residualMedianMs = Number(plan.residualMedianMs ?? Number.POSITIVE_INFINITY);\n  const residualP90Ms = Number(plan.residualP90Ms ?? Number.POSITIVE_INFINITY);\n  const offsetMs = Number(plan.offsetMs || 0);\n  const ratio = Number(plan.ratio || 1);\n  const ratioDelta = Math.abs(ratio - 1);\n  const cfg = config.timingEvidence;\n  const structurallyCompatible = Number(plan.sourceCueCount || 0) >= cfg.minCues\n    && Number(plan.referenceCueCount || 0) >= cfg.minCues\n    && cueRatio >= cfg.minCueRatio\n    && agreement >= cfg.minTemporalAgreement\n    && coverage >= cfg.minAnchorCoverage\n    && residualMedianMs <= cfg.maxResidualMedianMs\n    && residualP90Ms <= cfg.maxResidualP90Ms;\n  const aligned = structurallyCompatible\n    && Math.abs(offsetMs) <= cfg.alignedMaxOffsetMs\n    && ratioDelta <= cfg.alignedMaxRatioDelta;\n  const verdict = aligned ? 'aligned' : (structurallyCompatible ? 'repairable' : 'incompatible');\n  const structuralScore = Math.round(\n    agreement * 3500 + coverage * 2500 + cueRatio * 1500 + confidence * 15\n    - Math.min(1200, Math.abs(offsetMs) / 50)\n    - Math.min(1200, ratioDelta * 30000),\n  );\n  const rankScore = Math.max(0, (verdict === 'aligned' ? 30000 : verdict === 'repairable' ? 15000 : 0) + structuralScore);\n  const safeAutoAlign = cfg.safeAutoAlign\n    && verdict === 'repairable'\n    && confidence >= cfg.safeMinConfidence\n    && agreement >= cfg.safeMinAgreement\n    && coverage >= cfg.safeMinCoverage\n    && cueRatio >= cfg.safeMinCueRatio\n    && residualMedianMs <= cfg.safeMaxResidualMedianMs\n    && residualP90Ms <= cfg.safeMaxResidualP90Ms\n    && Math.abs(offsetMs) <= cfg.safeMaxOffsetMs\n    && ratio >= cfg.safeMinRatio\n    && ratio <= cfg.safeMaxRatio\n    && plan.enabled === true;\n  return {\n    measured: true, exactVideoHash: true, verdict, rankScore, safeAutoAlign,\n    confidence, temporalAgreement: agreement, anchorCoverage: coverage, cueRatio,\n    residualMedianMs: Number.isFinite(residualMedianMs) ? residualMedianMs : null,\n    residualP90Ms: Number.isFinite(residualP90Ms) ? residualP90Ms : null,\n    offsetMs, ratio, strategy: plan.strategy || null,\n  };\n}\n\n'''
s=rep(s,marker,helpers+marker,'timing helpers')
# Extend signature.
s=rep(s,"  preflightImpl = preflightSubtitleCandidate,\n  cacheGetImpl", "  preflightImpl = preflightSubtitleCandidate,\n  referencePreflightImpl = preflightTimingReferenceCandidate,\n  cacheGetImpl",'reference impl option')
# Add reference inspection after target inspections.
anchor='''  await Promise.all(targets.map(async item => {\n    const key = candidateKey(item, search);\n    const outcome = await inspectOne(item, search, { preflightImpl, cacheGetImpl, cacheSetImpl });\n    inspected.set(key, outcome);\n  }));\n\n  const decorated = ranked.map(item => {'''
insert='''  await Promise.all(targets.map(async item => {\n    const key = candidateKey(item, search);\n    const outcome = await inspectOne(item, search, { preflightImpl, cacheGetImpl, cacheSetImpl });\n    inspected.set(key, outcome);\n  }));\n\n  const exactReference = config.timingEvidence.enabled\n    ? targets.map(item => item?.exactTimingReference).find(reference => reference?.exactVideoHash)\n    : null;\n  const referenceOutcome = exactReference\n    ? await inspectTimingReference(exactReference, { referencePreflightImpl, cacheGetImpl, cacheSetImpl })\n    : null;\n\n  const decorated = ranked.map(item => {'''
s=rep(s,anchor,insert,'reference inspection')
# Decorated return: compute actual evidence.
old='''    return {\n      ...item,\n      ...(measuredQuality ? { quality: measuredQuality, qualityScore: measuredQuality.score } : {}),\n      accuracyPreflight: outcome,\n    };'''
new='''    let timing = null;\n    let safeTimingSyncPlan = null;\n    if (\n      config.timingEvidence.enabled\n      && item?.exactTimingReference?.exactVideoHash\n      && outcome?.timingProfile\n      && referenceOutcome?.timingProfile\n    ) {\n      const plan = deriveReferenceSyncPlanFromProfiles(outcome.timingProfile, referenceOutcome.timingProfile, {\n        minCues: 4, minCueRatio: 0, minConfidence: 0, minTemporalAgreement: 0, minAnchorCoverage: 0,\n        maxAnchors: 48, piecewise: true, dtwEnabled: true, dtwMaxCues: config.timingEvidence.maxCues,\n      });\n      timing = measuredTimingEvidence(plan);\n      if (timing.safeAutoAlign) {\n        safeTimingSyncPlan = {\n          ...plan, enabled: true, verified: true, method: 'exact-hash-reference-safe', exactVideoHash: true,\n        };\n      }\n    }\n    return {\n      ...item,\n      ...(measuredQuality ? { quality: measuredQuality, qualityScore: measuredQuality.score } : {}),\n      ...(timing ? { actualTimingEvidence: timing } : {}),\n      ...(safeTimingSyncPlan ? { safeTimingSyncPlan, safeTimingReference: item.exactTimingReference } : {}),\n      accuracyPreflight: outcome,\n    };'''
s=rep(s,old,new,'decorate timing evidence'); w(p,s)

# Accuracy ordering: measured timing beats metadata timing/family when both candidates were measured.
p='src/utils/accuracyFirst.js'; s=r(p)
insert_after='''function timingReferenceRank(item) {\n  const evidence = item?.timingReferenceEvidence;\n  if (!evidence?.exactVideoHash) return 0;\n  const score = Number(evidence.matchScore || 0);\n  return Math.max(0, Math.min(10_000, Number.isFinite(score) ? score : 0));\n}\n'''
addition=insert_after+'''\nfunction actualTimingRank(item) {\n  const evidence = item?.actualTimingEvidence;\n  if (!evidence?.measured || !evidence?.exactVideoHash) return null;\n  const score = Number(evidence.rankScore);\n  return Number.isFinite(score) ? score : 0;\n}\n'''
s=rep(s,insert_after,addition,'actual timing rank helper')
anchor='''    const hardConflictDelta = hardConflictCount(a) - hardConflictCount(b);\n    if (hardConflictDelta) return hardConflictDelta;\n\n    // An English subtitle returned for the exact video hash describes the actual playback'''
insert='''    const hardConflictDelta = hardConflictCount(a) - hardConflictCount(b);\n    if (hardConflictDelta) return hardConflictDelta;\n\n    // Actual cue-timeline evidence is stronger than release-name/source-family guesses.\n    // Compare it only when both candidates were measured, so a transient reference/preflight\n    // failure never blindly penalizes an unmeasured subtitle.\n    const aActualTiming = actualTimingRank(a);\n    const bActualTiming = actualTimingRank(b);\n    if (aActualTiming !== null && bActualTiming !== null) {\n      const actualTimingDelta = bActualTiming - aActualTiming;\n      if (actualTimingDelta) return actualTimingDelta;\n    }\n\n    // An English subtitle returned for the exact video hash describes the actual playback'''
s=rep(s,anchor,insert,'actual timing ordering')
s=rep(s,"  if (evidenceRank(item) > 0 || timingReferenceRank(item) > 0) return true;","  if (evidenceRank(item) > 0 || actualTimingRank(item) !== null || timingReferenceRank(item) > 0) return true;",'strong timing evidence'); w(p,s)

# Original Stremio options transparently use only the conservatively approved exact-hash correction.
p='src/utils/stremio.js'; s=r(p)
anchor='''function referenceForProxy(baseUrl, item) {\n  const ref = item.referenceSubtitle;\n  const download = ref?.download || ref?.url;\n  if (!download) return null;\n  return {\n    url: download.startsWith('/') ? `${baseUrl}${download}` : download,\n    provider: ref.provider,\n    name: ref.name || ref.releaseName || ref.fileName || 'English reference',\n  };\n}\n'''
addition=anchor+'''\nfunction safeTimingReferenceForProxy(baseUrl, item) {\n  if (!item?.safeTimingSyncPlan?.enabled || !item?.safeTimingSyncPlan?.exactVideoHash) return null;\n  const ref = item.safeTimingReference;\n  const download = ref?.download || ref?.url;\n  if (!ref?.exactVideoHash || !download) return null;\n  return {\n    url: download.startsWith('/') ? `${baseUrl}${download}` : download,\n    provider: ref.provider,\n    providerId: ref.providerId || ref.fileId || ref.id,\n    name: ref.releaseName || ref.fileName || ref.name || 'Exact-hash timing reference',\n  };\n}\n\nfunction safeTimingFallbacks(baseUrl, candidates = []) {\n  return candidates.map(candidate => {\n    const reference = safeTimingReferenceForProxy(baseUrl, candidate);\n    return {\n      ...candidate,\n      syncPlan: reference ? candidate.safeTimingSyncPlan : null,\n      reference,\n    };\n  });\n}\n'''
s=rep(s,anchor,addition,'safe reference helper')
s=rep(s,"  if (item.quality?.score) badges.push(`✓ Q${item.quality.score}`);","  if (item.actualTimingEvidence?.verdict === 'aligned') badges.push('🎯 Timed');\n  else if (item.safeTimingSyncPlan?.enabled) badges.push('⏱ Auto-aligned');\n  if (item.quality?.score) badges.push(`✓ Q${item.quality.score}`);",'timing badges')
old='''    const originalUrl = proxiedSubtitleUrl(baseUrl, item, null, null, search, rankedFallbacks);\n    if (!originalUrl) continue;\n    eligible.push({ item, index, rankedFallbacks, originalUrl });'''
new='''    const safeReference = safeTimingReferenceForProxy(baseUrl, item);\n    const safePlan = safeReference ? item.safeTimingSyncPlan : null;\n    const originalUrl = proxiedSubtitleUrl(\n      baseUrl, item, safePlan, safeReference, search, safeTimingFallbacks(baseUrl, rankedFallbacks),\n    );\n    if (!originalUrl) continue;\n    eligible.push({ item, index, rankedFallbacks, originalUrl });'''
s=rep(s,old,new,'original safe alignment'); w(p,s)

# Permanent regression tests: generic, no title/IMDb special-casing.
p='src/tests/actualTimingEvidence.test.js'
w(p,'''import test from 'node:test';\nimport assert from 'node:assert/strict';\nimport { applyAccuracyPreflight } from '../services/accuracyPreflight.js';\nimport { buildTimingProfile } from '../utils/referenceSync.js';\n\nfunction srt(starts) {\n  return starts.map((start, i) => {\n    const end = start + 1200 + (i % 3) * 180;\n    const time = ms => { const h=Math.floor(ms/3600000), m=Math.floor((ms%3600000)/60000), s=Math.floor((ms%60000)/1000), x=ms%1000; return `${String(h).padStart(2,'0')}:${String(m).padStart(2,'0')}:${String(s).padStart(2,'0')},${String(x).padStart(3,'0')}`; };\n    return `${i+1}\\n${time(start)} --> ${time(end)}\\nسطر عربي رقم ${i+1}`;\n  }).join('\\n\\n');\n}\n\nconst baseStarts = Array.from({ length: 60 }, (_, i) => i * 3700 + (i % 7) * 190);\nconst referenceProfile = buildTimingProfile(srt(baseStarts));\nconst reference = { provider:'opensubtitles', providerId:'999', download:'/downloads/opensubtitles/999.srt', exactVideoHash:true, releaseName:'Exact.Hash.Reference' };\nfunction candidate(id, score=500) { return { id, provider:'opensubtitles', providerId:id==='aligned'?'101':'102', fileId:id==='aligned'?'101':'102', download:`/downloads/opensubtitles/${id==='aligned'?'101':'102'}.srt`, lang:'ara', score, releaseName:`Movie.2026.2160p.BluRay.${id}`, releaseMatch:{targetFields:3,criticalMismatches:0,tier:3,priority:30000,mismatched:[]}, exactTimingReference:reference }; }\nconst noCache={cacheGetImpl:async()=>null,cacheSetImpl:async()=>{}};\n\ntest('actual cue timing outranks a higher textual score when both use the same exact-hash reference', async () => {\n  const profiles={\n    aligned: buildTimingProfile(srt(baseStarts)),\n    shifted: buildTimingProfile(srt(baseStarts.map(v=>v+9000))),\n  };\n  const results=await applyAccuracyPreflight([candidate('shifted',900),candidate('aligned',400)],{filename:'Movie.2026.2160p.BluRay.Remux.mkv',videoHash:'abc'}, {\n    ...noCache,\n    preflightImpl:async item=>({quality:{valid:true,score:90,reasons:[]},timingProfile:profiles[item.id]}),\n    referencePreflightImpl:async()=>({timingProfile:referenceProfile}),\n  });\n  assert.equal(results[0].id,'aligned');\n  assert.equal(results[0].actualTimingEvidence.verdict,'aligned');\n  assert.equal(results[1].actualTimingEvidence.verdict,'repairable');\n});\n\ntest('high-confidence shifted timing receives a safe exact-hash correction plan', async () => {\n  const shifted=buildTimingProfile(srt(baseStarts.map(v=>v+6000)));\n  const results=await applyAccuracyPreflight([candidate('shifted')],{filename:'Movie.2026.2160p.BluRay.Remux.mkv',videoHash:'abc'}, {\n    ...noCache,\n    preflightImpl:async()=>({quality:{valid:true,score:90,reasons:[]},timingProfile:shifted}),\n    referencePreflightImpl:async()=>({timingProfile:referenceProfile}),\n  });\n  assert.equal(results[0].actualTimingEvidence.verdict,'repairable');\n  assert.equal(results[0].safeTimingSyncPlan?.enabled,true);\n  assert.equal(results[0].safeTimingSyncPlan?.exactVideoHash,true);\n});\n\ntest('a clearly different cut is marked incompatible rather than trusted by BluRay metadata alone', async () => {\n  const shortProfile=buildTimingProfile(srt(baseStarts.slice(0,20)));\n  const results=await applyAccuracyPreflight([candidate('shifted')],{filename:'Movie.2026.2160p.BluRay.Remux.mkv',videoHash:'abc'}, {\n    ...noCache,\n    preflightImpl:async()=>({quality:{valid:true,score:90,reasons:[]},timingProfile:shortProfile}),\n    referencePreflightImpl:async()=>({timingProfile:referenceProfile}),\n  });\n  assert.equal(results[0].actualTimingEvidence.verdict,'incompatible');\n  assert.equal(Boolean(results[0].safeTimingSyncPlan),false);\n});\n\ntest('reference outage fails open and preserves deterministic existing ranking', async () => {\n  const first=candidate('shifted',700), second=candidate('aligned',600);\n  const results=await applyAccuracyPreflight([first,second],{filename:'Movie.2026.2160p.BluRay.Remux.mkv',videoHash:'abc'}, {\n    ...noCache,\n    preflightImpl:async()=>({quality:{valid:true,score:90,reasons:[]},timingProfile:referenceProfile}),\n    referencePreflightImpl:async()=>{throw new Error('reference unavailable');},\n  });\n  assert.deepEqual(results.map(x=>x.id),['shifted','aligned']);\n  assert.equal(results.some(x=>x.actualTimingEvidence),false);\n});\n''')

# Reference profile API tests.
p='src/tests/referenceTimingProfile.test.js'
w(p,'''import test from 'node:test';\nimport assert from 'node:assert/strict';\nimport { buildTimingProfile, deriveReferenceSyncPlanFromProfiles } from '../utils/referenceSync.js';\n\nconst fmt=ms=>{const h=Math.floor(ms/3600000),m=Math.floor(ms%3600000/60000),s=Math.floor(ms%60000/1000),x=ms%1000;return `${String(h).padStart(2,'0')}:${String(m).padStart(2,'0')}:${String(s).padStart(2,'0')},${String(x).padStart(3,'0')}`};\nconst make=(shift=0)=>Array.from({length:50},(_,i)=>{const st=i*4100+(i%5)*130+shift;return `${i+1}\\n${fmt(st)} --> ${fmt(st+1400+(i%4)*100)}\\nx`;}).join('\\n\\n');\n\ntest('timing profile is bounded while preserving original cue count',()=>{const p=buildTimingProfile(make(),32);assert.equal(p.cueCount,50);assert.ok(p.cues.length<=32);});\ntest('profile comparison detects a global shift without release metadata',()=>{const a=buildTimingProfile(make(5000)),b=buildTimingProfile(make(0));const plan=deriveReferenceSyncPlanFromProfiles(a,b,{minCues:4,minCueRatio:0,minConfidence:0,minTemporalAgreement:0,minAnchorCoverage:0,dtwEnabled:true,piecewise:true});assert.ok(Math.abs(plan.offsetMs+5000)<1200);assert.ok(plan.temporalAgreement>0.75);});\n''')

# Release notes.
p='CHANGELOG.md'; s=r(p); entry='''## 4.5.0 - 2026-09-07\n\n- Add actual cue-timeline evidence against an English subtitle matched to the exact video hash; release-name/source-family similarity is now a fallback rather than the strongest available timing signal.\n- Reuse the bounded Accuracy Preflight downloads to compare Arabic cue structure with the exact-hash reference using temporal anchors + DTW.\n- Rank already-aligned Arabic subtitles above merely repairable or incompatible candidates, even when textual/provider scores favor the wrong file.\n- Safely auto-align only repairable subtitles that meet strict exact-hash timing thresholds; generic filename-based auto-sync remains opt-in/off.\n- Add generic regression tests for same-family/different-timeline, shifted, different-cut, and reference-outage cases without title-specific rules.\n\n'''; s=entry+s; w(p,s)
p='README.md'; s=r(p); s=rep(s,'# m7md Arabic Resolver v4.4.2','# m7md Arabic Resolver v4.5.0','README version'); marker='## ما الجديد في 4.4.2\n'; section='''## ما الجديد في 4.5.0\n\n- أصبح اختيار الترجمة يقيس **توقيت الـcues الفعلي** مقابل مرجع إنجليزي مطابق لـ`videoHash` نفسه، بدل الاكتفاء باسم الإصدار وBluRay/WEB/FPS.\n- تستخدم أفضل الترجمات العربية فحصًا زمنيًا محدودًا (Temporal Anchors + DTW) وتظهر الترجمة المصطفّة فعليًا قبل ترجمة تحمل اسم إصدار أفضل لكنها مختلفة زمنيًا.\n- إذا لم توجد ترجمة مصطفّة لكن وُجدت ترجمة قابلة للتصحيح بثقة عالية جدًا، يمكن تصحيح توقيتها تلقائيًا **فقط** بمرجع Exact-Hash لنفس الفيديو.\n- المزامنة العامة المبنية على التخمين أو اسم الملف ما زالت مغلقة افتراضيًا.\n- لا توجد قواعد خاصة بفيلم أو مسلسل بعينه؛ الاختبارات تغطي اختلاف الـcuts والنسخ كحالات عامة.\n\n'''; s=rep(s,marker,section+marker,'README release section'); w(p,s)

print('v4.5.0 actual timing evidence applied')