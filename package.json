{
  "name": "m7md-arabic-stremio-subtitles",
  "version": "3.1.0",
  "type": "module",
  "private": true,
  "description": "Private direct Stremio Arabic subtitles add-on with Personal Subtitle Vault, optional YIFY fallback provider, strict private ranking defaults, hash-first matching, smart cache, metrics, server-side Arabic encoding normalization, and deterministic reference auto-sync without ذكاء اصطناعي.",
  "engines": {
    "node": ">=24.12 <25"
  },
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js",
    "companion:scan": "node src/companion/mediaCompanion.js scan",
    "test": "node --test src/tests/*.test.js",
    "test:contracts": "node --test src/tests/liveProviders.test.js"
  },
  "dependencies": {
    "@napi-rs/lzma": "1.5.1",
    "compression": "^1.8.1",
    "cors": "^2.8.6",
    "express": "^4.19.2",
    "express-rate-limit": "^7.3.1",
    "fflate": "0.8.3",
    "helmet": "^7.1.0",
    "ioredis": "^5.4.1",
    "morgan": "^1.10.0",
    "undici": "^7.25.0"
  }
}
