# Timing regression corpus

Each fixture in `timing-regressions.json` represents a real or distilled timing-selection incident. The expected winner must remain stable unless stronger evidence such as an exact video hash is introduced. Add new user-reported mismatch cases here before changing ranking heuristics so future releases cannot silently regress them.
